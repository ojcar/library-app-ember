
import { libraryTitle } from 'library-app-ember/helpers/library-title';
import { module, test } from 'qunit';

module('Unit | Helper | library title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = libraryTitle([42]);
  assert.ok(result);
});

