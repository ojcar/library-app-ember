
import { libraryLibraryTitle } from 'library-app-ember/helpers/library/library-title';
import { module, test } from 'qunit';

module('Unit | Helper | library/library title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = libraryLibraryTitle([42]);
  assert.ok(result);
});

